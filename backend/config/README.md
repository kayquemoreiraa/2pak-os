# config/

Pasta destinada às configurações da aplicação.

Conteúdo previsto para a próxima etapa:
- `database.js` — configuração e criação do pool de conexão MySQL (host, porta, usuário, senha, nome do banco), lendo as variáveis de `.env`.
- Futuras configurações globais (ex: CORS detalhado, limites de upload, etc.), caso necessário.

Esta pasta ainda está vazia propositalmente — será populada na etapa de conexão com o banco de dados.
